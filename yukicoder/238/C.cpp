#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
    int N,M,K,p,q;
    cin >> N >> M >> K >> p >> q;
    vector<int> b(N);
    rep(i, N) cin >> b[i];

    


}